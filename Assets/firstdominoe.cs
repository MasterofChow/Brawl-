using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Starts the game off

public class firstdominoe : MonoBehaviour
{
    // Start is called before the first frame update
    void Awake()
    {
        continueBackgroundMusic.started = true;
    }
}
